auths:
  global:
    version: 2
    community: ${SNMP_COMMUNITY}

modules:
  if_mib:
    walk:
      - sysUpTime
      - ifTable
      - ifXTable
    max_repetitions: 25
    retries: 3
    timeout: 10s
