# TODO（功能与现场验收清单）

更新时间：2026-08-03

状态说明：`[x]` 表示代码或本次现场证据已经验证；`[ ]` 表示仍需开发、故障注入、其他型号或完整部署环境验证。单个现场通过不会自动代表所有设备型号均通过。

## 2026-08-03 现场验收记录

### 当前运行环境

- [x] 服务器运行版本为 `55325fe`，本次检查时 Compose 主服务均处于运行状态。
- [x] `platform-api` 生产容器确认使用 Python 3.13.14、`telnetlib3` 4.0.5 和 iPerf3 3.18。
- [x] Prometheus 最近一次配置重载成功；核心、汇聚/接入、防火墙、ISP、SNMP 等已配置抓取目标均正常。
- [x] Grafana API、Prometheus 数据源和 3/3 个仓库 Dashboard 正常；LibreNMS dispatcher 正常，`validate.php` 无真实 FAIL。
- [x] 飞书桥健康检查 `ready=true`、`deadWatchers=[]`，8 个后台线程均存活且最近错误为空。
- [x] 飞书测试卡实际收到；本次投递耗时 0.643 秒，Webhook 返回成功。
- [x] `isp-bandwidth` 线程当前存活并持续输出流量，未再出现此前的线程停止状态。
- [ ] 飞书自建应用投递链路仍未完全通过：测试时群列表接口返回 `HTTP 400`，随后自动回退 Webhook 并成功。需检查应用权限、群配置或接口请求，确保需要交互能力时不依赖 Webhook 回退。
- [ ] 当前赛前体检显示 `player-ping` 为 0，符合非比赛期间没有选手目标的状态；正式赛前仍必须确认比赛所需机位全部生成并在线。
- [ ] 当前配置的 3 台服务器中仅 1 台可 Ping，`server1`、`server2` 离线。如果它们确定不再使用，应在下一项目配置中删除或停用，避免赛前体检持续报失败。

### C3850 DHCP 实机验收

- [x] 设备：WS-C3850-12XS，IOS XE 16.12.11，当前权限级别 15。
- [x] Telnet 只读采集成功，无解析警告，单次耗时 1.74 秒。
- [x] 解析出 39 个地址池：总地址 10674、已租用 171、排除 636、可用 9867、使用率 1.7%、冲突 0。
- [x] 解析结果与交换机原始输出一致：`Address pools 39`、`Automatic bindings 171`，冲突表为空；各池的租用和排除数量也能对应。
- [x] 日常面板采集未执行 `show ip dhcp binding`；完整租约表仍只允许人工按需查询。
- [x] 单次采集前后 CPU 五分钟均值保持 9%，一分钟均值从 9% 到 10%；未观察到持续负荷上涨。
- [ ] 单次采集后的五秒 CPU 从 8% 短时升到 20%。数值仍可接受，但正式关闭负荷验收前应再连续观察多轮 60 秒页面刷新，确认旧设备没有累积波动。
- [ ] C9300 尚未提供同样的实机输出；如果后续项目让 C2960 承担 DHCP，也需单独验证。
- [ ] 普通密码、用户名+密码、Enable 密码三种 Telnet 登录方式尚未分别完成现场验证。

### 当前防火墙与 ISP 基线

- [x] `firewall-snmp` 1/1 抓取正常，当前识别到 4 条 ISP/WAN 链路。
- [x] `infra-fw-snmp` 1/1、`infra-fw-unit-snmp` 2/2、`infra-fw-ping` 1/1、`infra-isp-ping` 4/4 正常。
- [x] ISP 自动发现持续稳定生成 `telcom-1000M`、`telcom-100M`、`telcom-100M-长期`、`unicom-1000M` 四个目标，未出现无安全网关目标。
- [x] ISP 带宽线程持续输出流量和各线路阈值，当前没有数据中断或虚假超大速率日志。
- [ ] 尚未完成物理防火墙单机拔线/恢复、VIP 不误报、HA 主备切换、SNMP 中断两分钟和计数器重置等故障注入。
- [ ] 尚未分别完成 Hillstone HA 与 WatchGuard HA 的独立验收；当前证据只代表本现场现有设备。

## P0：正式交付或新项目前必须确认

- [ ] **补齐 Cisco DHCP 型号和登录方式验收**
  - C3850 单次采集、解析对照和短时 CPU 已通过，详见上方记录。
  - 至少再覆盖常用 C9300；现场仍可能用 C2960 做 DHCP 时再补 C2960。
  - 验证三种 Telnet 登录方式及非 15 权限、分页、空池、不支持统计命令等情况。
  - 连续观察多轮页面刷新期间的 CPU，继续禁止后台定时读取完整 binding 表。

- [ ] **补齐防火墙 ISP 故障注入与双厂商验收**
  - 当前现场的 SNMP、4 条 WAN、自动网关发现和实时速率基线已通过。
  - Hillstone HA 与 WatchGuard HA 各验收一次。
  - 拔一台物理防火墙，确认单机离线/恢复告警且 VIP 不误报。
  - 验证主备切换/重启时只忽略计数器异常，不出现几十 Gbps 假带宽卡。
  - 中断 SNMP 或让接口名不匹配两分钟，确认收到“外网流量采集中断”和恢复卡。
  - 核对每条 WAN 名称与 100M/1000M 等阈值绑定关系。

- [ ] **在干净 Linux 服务器做完整部署和恢复验收**
  - 当前运行镜像内的 Python 3.13、`telnetlib3`、iPerf3 已确认。
  - 仍需从仓库全新构建，不复用旧镜像。
  - 验证 Docker 重启后赛事配置、DHCP 私密设置、告警状态和历史数据保持。
  - 做一次离线包安装，确认无外网现场只需预装 Docker、Compose 插件和 Python 3。

- [ ] **做一次完整现场告警回归**
  - 飞书桥健康和测试卡投递已经通过，但测试卡不能代替真实事件回归。
  - 新设备上线、设备离线、普通恢复、端口/链路、广播或组播风暴、LibreNMS 告警桥接各测一次。
  - 验证设备离线满 48 小时后旧周期清理，再次出现只发送“新设备上线”。
  - 验证清理停用设备不会误删仍在使用的 LibreNMS 设备。
  - 正式环境不得使用已公开的测试 Webhook。

- [ ] **从实际项目出口手动验证公共 iPerf3 节点**
  - 分别检查香港、新加坡、土耳其和印度尼西亚至少一个节点。
  - 记录可用服务器、端口、日期和现场出口；只在空闲窗口测速。

## P1：下一轮优先改进

- [ ] **修复飞书自建应用群解析 HTTP 400**
  - 当前普通告警依靠 Webhook 回退仍可正常收到，投递仅 0.643 秒。
  - 需要确认应用机器人群列表权限、目标群标识和请求参数，使交互卡链路本身成功。
  - 修复后健康信息应能区分“应用投递成功”和“Webhook 回退成功”，避免仅凭收到消息误判应用链路正常。

- [ ] **告警投递改为队列化异步重试**
  - 当前正常飞书响应很快，没有实际阻塞迹象。
  - 风险只在飞书超时、限流或网络异常时出现：调用告警发送的对应 watcher 会等待网络超时和重试。
  - 后续可采用投递队列、按事件退避和队列上限；不要为此增加交换机轮询。

- [ ] **为 iPerf3 增加停止测速**
  - 当前“取消”只取消开始前确认，开始后只能等待完成或最长 60 秒超时。
  - 后端改为可管理子进程，并提供仅终止当前任务的接口。

- [ ] **为 iPerf3 增加任务编号并隔离多个页面状态**
  - 保持平台同一时间只允许一项测速，但为每次任务生成 ID。

- [ ] **保存少量 iPerf3 历史**
  - 只保留最近 10～20 次时间、地区、服务器、方向、速率、传输量、重传和每秒数据。

- [ ] **把公共 iPerf3 节点从前端代码移到版本化配置**
  - 显示最后核验日期，支持轻量 DNS/TCP 可达性检查和人工切换备用节点。

- [ ] **把 C3850 实机输出脱敏后加入 DHCP 测试夹具，并补 C9300/C2960**
  - 当前 C3850 原始输出和解析对照已经取得，但尚未作为脱敏 fixture 提交。
  - 覆盖分页、无冲突、空池、不支持统计命令和非 15 权限。
  - 页面已有上次采集时间和缓存/实时状态；仍需增加采集耗时显示。

- [ ] **让 CI 同时验证 Python 3.13**
  - 当前生产容器 3.13.14 已实机确认，但 GitHub CI 仍主要跑 Python 3.11。
  - 3.13 测试必须覆盖 `telnetlib3` 分支。

- [ ] **增加真实 Linux 轻量冒烟测试**
  - 检查 Compose、容器健康、登录、DHCP 设置保存、iPerf3 和飞书桥健康。
  - 默认不连接交换机、不发正式飞书消息、不运行带宽测试。

## P2：已讨论、后续再做

- [ ] 飞书手动查询 LibreNMS 光模块 Tx/Rx dBm；不做自动光功率告警。
- [ ] 应用/树莓派机器人状态接入，先明确业务数据接口。
- [ ] 组播业务按需检查，接入前先评估 C2960 CPU，默认不高频轮询。
- [ ] 拓扑和接口详情增强前做负荷评估，按需读取并缓存，避免全量高频 FDB/ARP/接口轮询。

## P3：代码质量和维护性

- [ ] **拆分过大的源文件**
  - `bigscreen/app.js` 可按控制台、DHCP、iPerf3、告警和页面生命周期拆分。
  - `platform-api.py` 可按认证/配置、部署、网络工具和 HTTP 路由拆分。
  - `alertmanager-feishu-bridge.py` 可按归一化、设备生命周期、卡片和发送队列拆分。

- [ ] **把关键前端流程从字符串检查升级为行为测试**
  - 重点覆盖 iPerf3 节点切换、字段锁定、确认、进度轮询和 DHCP 页面刷新。

- [ ] **逐步清理 ShellCheck warning，再提高 CI 严格度**。
- [ ] **完成运行数据备份—重装—恢复演练并记录步骤**。

## 已完成的历史代码审查项

- [x] 在线卡发送网络请求时不再持有设备在线状态锁。
- [x] syslog 失败事件有复检、次数上限、过期时间和恢复清理。
- [x] 聚合链路会过滤管理关闭口，并处理旧聚合口消失后的恢复/清理。
- [x] 回滚 guard 快照不会再次成为普通回滚目标，避免坏状态间来回跳。
- [x] 快照、事务状态和 apply-status 均有保留上限。
- [x] 登录失败按 IP 和账号计数并进入锁定窗口。
- [x] ICMP 在线但 SNMP 暂时失败的交换机仍保留为占位监控目标。
- [x] 选手目标在存活校验后做最终座位去重，不再先去重丢掉在线候选。
- [x] Telnet 使用严格设备提示符，发送 `terminal length 0` 并处理 `--More--` 分页。
- [x] DHCP 采集有并发锁；生产 Python 3.13 镜像已经安装 `telnetlib3`。

## 暂不做 / 保持现状

- 不迁移 DHCP 到 Kea/Stork；Cisco 核心继续提供 DHCP，页面只读展示。
- 不恢复“座位核对”页面。
- 不恢复“访问模式 / 外网访问 / 公网基础 URL”字段。
- 不自动持续运行 iPerf3，不把公共测速当作运营商带宽验收的唯一依据。
- 暂不增加 UDP 打流；需要排查抖动、丢包或组播时再单独设计。
- 暂不强制 Telnet 改 SSH，但账号权限应尽量只满足只读命令。
- 不做自动光功率告警。
- 不新增高频交换机轮询，尤其避免在旧 C2960 上增加全量采集。
- 比赛大屏继续只认交换机端口 `description` 中的 `team X-Y` 机位；非比赛期间选手离线不要求保留为在线目标。

## 每次准备合并时固定检查

- [ ] `git diff --check` 无空白或冲突标记问题。
- [ ] Python、前端、ShellCheck、Compose 和 Grafana JSON 检查全部通过。
- [ ] 本地 Docker 使用当前源码重新构建，页面版本与工作区一致。
- [ ] 不提交 `.env`、密码、Webhook、运行状态卷或现场私密清单。
- [ ] 提交说明列出用户可见变化、部署步骤、回滚办法和未完成事项。
