# Agent 工作协议

本文件适用于整个 `monitor-autoconfig` 仓库。使用中文沟通和维护工程文档。
标准入口文件名为 `AGENTS.md`；不要另建 `agent.md` 或复制多份相互冲突的规则。

## 用户意图与边界

- 如果需求存在逻辑、根本性错误，或与原需求明显不同，先指出问题、影响和推荐方案；只有用户明确坚持原方案，才在允许范围内继续该方案。普通实现细节自行判断。
- 每批只处理明确范围，先给出目标、验收条件和受影响模块；不要因“还能优化”反复改动已验收模块。
- 以当前会话的明确指示为准；持久化文档用于恢复事实和约束。发现文档与 Git、代码或用户反馈冲突时，记录来源并核实，不能默默把推测当结论。
- 默认由当前 Agent 串行实施、自查、提交。仅在用户明确要求时启用子 Agent；多人同时工作时也只能有一个 Git 写入者。

## 每次开始或恢复上下文

1. 阅读本文件、[当前状态](docs/STATUS.md)、[项目约束](docs/PROJECT_CONTEXT.md)。
2. 阅读 STATUS 指向的活跃迭代记录，以及本次相关的 [工程标准](docs/ENGINEERING.md)、[Git 规范](docs/GIT_WORKFLOW.md)、[文档规范](docs/DOCUMENTATION.md)。按需读源码和专题文档，不把全部历史重新灌入上下文。
3. 检查实际分支、工作区、暂存区和近期提交；修改前记录基线。远端跟踪引用可能过期，不能据此声称 GitHub 已同步。
4. 有他人改动时保留原样，明确归属，只提交本轮文件；暂存区已有他人内容时先解决归属，不混入提交。
5. 从记录中的“下一步”继续。没有新故障或新证据，不重做已完成审计，不重审冻结 ISP。

## 工程与安全底线

- 遵守 [工程标准](docs/ENGINEERING.md)：小步修改、清晰模块边界、输入和资源有界、超时明确、失败可诊断、测试覆盖真实行为。
- Golden appliance 已冻结且故意移除 `.git`；禁止访问或修改 Golden。公司服务器由用户执行部署命令，Agent 不自动连接。
- 不制作 Linux 验证包或独立测试镜像。现有本地测试及 GitHub CI 保留，公司服务器承担部署和运行验收。
- ISP/HA 模型及凭据处理遵循 [项目约束](docs/PROJECT_CONTEXT.md)。真实生产故障以外不得重新审查或重构 ISP。
- 公司 Auto Delete 四开关必须保持 `true / true / true / false`，对应顺序见项目约束。未经用户明确批准，禁止将 `DEVICE_AUTO_DELETE_DRY_RUN` 改为 `false`。
- 不在生产执行真实设备 DELETE，不为测试主动发送飞书消息。不得删除、移动、提交服务器既有 `.env` 备份、日志、运行目录或未跟踪的 `VERSION`。
- 不打印或提交真实凭据、完整生产配置、原始事故内容。测试使用脱敏样例和临时目录。

## Git 协议

- 仅在本地主检出 `main` 串行提交；不创建 feature branch、worktree、detached HEAD 或 PR。
- 已授权范围内完成验证、更新文档后，创建普通 commit 并普通 push 到 `origin/main`，无需每轮重复询问。
- 不 amend、rebase、force push、改写历史，不用 `reset --hard`、`clean` 或自动 stash 掩盖冲突。补丁使用追加 commit；分歧处理见 [Git 规范](docs/GIT_WORKFLOW.md)。
- 精确暂存本轮文件并审查暂存 diff；不得盲用 `git add .` / `git add -A`。

## 每轮结束必须执行（含文档迭代）

1. 完成范围内实现和自查；运行与风险匹配的验证。缺少 Linux Python 3.13、Node 20、ShellCheck 等环境时记录未验证项，不仅因缺工具阻止 push；已发现的真实阻断必须处理。
2. 同步更新行为/运维文档、迭代记录与 `docs/STATUS.md`。关键决策、原因、修改范围、证据、失败/未测项、下一步、约束均须入库，不能只留在聊天中。
3. 执行 `git diff --check`，检查文档链接、命令路径和敏感信息；审查精确暂存内容，提交，再普通 push。验证远端目标提交与本地关系；失败则记录实际状态，不声称成功。
4. 严格区分“本地验证”“已提交”“已推送”“CI 通过”“已部署”“生产验收通过”。生产验收只能据用户反馈或实际授权执行结果记录；待验收可结束本地交付，不能标记业务批次已收口。
5. 涉及运行行为/部署的交付，在同一条回复中一次给齐 fetch/pull、部署、configured、源码/容器/HTTP 一致性、四开关和网页验收命令/步骤，可链接本次完整的入库操作手册。纯文档迭代明确不需要重启服务，已有待部署事项继续保留。
6. **先持久化并提交，再压缩上下文。** 若当前工具可调用上下文压缩，则在保存后执行；否则明确报告“交接已持久化，未执行客户端压缩”，由支持该功能的客户端入口触发。不得把普通摘要或声称遗忘当作实际压缩，不得因无法压缩跳过交付。
7. 最终回复给出结果、提交/推送状态、验证及限制、文档入口与唯一下一步。恢复时按本文件开头顺序读取，不依赖长聊天历史。

## Code Review Rules

- 优先报告可复现的正确性、兼容性、权限、数据丢失、删除安全、凭据泄露和部署误报问题，指出触发条件、影响与代码位置。
- 核查异步旧响应是否能覆盖新状态，超时/异常是否被误判成功，存储失败是否改变既有数据，UNKNOWN 是否错误进入删除流程。
- 核查测试是否验证外部行为和失败分支；避免只匹配实现字符串。格式偏好不作为阻断，不扩大到冻结模块。
